apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: node-exporter
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - cluster: app-0
        name: gke_kentaiso-330205_asia-northeast1_app-0
      - cluster: corp-0
        name: gke_kentaiso-330205_asia-northeast1_corp-0
      - cluster: misc-0
        name: gke_kentaiso-330205_asia-northeast1_misc-0
  template:
    metadata:
      name: "{{cluster}}-node-exporter"
    spec:
      project: default
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
      source:
        chart: prometheus-node-exporter
        repoURL: https://prometheus-community.github.io/helm-charts
        targetRevision: 4.17.0
        helm:
          releaseName: "{{cluster}}"
          values: |
      destination:
        name: "{{name}}"
        namespace: monitoring
